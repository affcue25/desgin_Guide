import { makeStyles } from "@mui/styles";
export const Style = makeStyles(() => ({
  margin: {
    display: "flex",
    marginTop: "5rem",
    // textAlign: "center",
  },

  marginBottom: {
    marginBottom: "3rem"
  },
  textField: {
    width: "100%",
    display: "grid"
  },

  profileMargin: {
    marginBottom: "3rem",
    fontWeight: "bold"
  }
}));

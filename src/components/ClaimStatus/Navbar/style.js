import { makeStyles } from "@mui/styles";
export const Style = makeStyles(() => ({
  logoGrid: {
    display: "flex",
  },
  FAQGrid: {
    justifyContent: "space-around",
  },
  fileClaimGrid: {
    textAlign: "right",
  },
}));

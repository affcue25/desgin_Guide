import { makeStyles } from "@mui/styles";
export const Style = makeStyles(() => ({
  title: {
    fontSize: "24px",
    fontWeight: "bold",
    fontFamily: "sans-serif",
    color: "#22225C",
  },

  fileClaimGrid: {
    textAlign: "right",
  },

}));
